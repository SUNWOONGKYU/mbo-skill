---
description: "mbo-천상(天上) — 모든 스킬·작업의 최상위 메타 스킬. MBO(목표에 의한 관리) 기반. 작업 시작 전 목표를 정의하고 PO 승인받아 통솔 대상 스킬들을 호출해 실행. 작업 완료 시 목표 달성 여부를 보고."
---

<!-- MBO_SPEC_VERSION: V3.12 -->
<!-- mbo-천상 = 前 mbo-코어5. V3.7 최상위 메타 스킬 격상. V3.8 전체 스킬 지도(42)·스무고개·자동 발동 편입. V3.9 스무고개 개선(조사 기반·조기수렴). V3.10 자기완결화 1차 — 외부 출처 제거·호출 동작 내장. V3.11 완전 자기완결 — 본문 외부 스킬 이름 0건·색인 부록 없이 단독 동작. V3.12 용어·양식 정합 — 기준 명칭을 "목표 달성 평가·검증 기준"으로 통일·용어 정의 절 신설, "MBO 파일"→"목표서 파일", PO(인간, 지휘자) 정의 통일, 부분달성/비코드 완료조건/검증 독립성/스무고개 단축조건/report 탐색규칙/원칙-스텝 매핑 보강. -->

# /mbo-천상 — 최상위 메타 스킬 (MBO 기반)

> **이 파일은 공개본**이다. `mbo-천상`은 원래 PO의 운영 환경에서 4 사신·다수 스킬을 통솔하는 확장판의 이름이며, 이 공개본은 그 **핵심(목표·승인·결과 관리 틀)만 자기완결 형태로 공개**한 것이다. 확장판 없이 이 파일 하나로 단독 동작한다. 설치 후 `/mbo`로 호출한다.

## 위상 — mbo-천상계 우주관

**mbo-천상은 모든 작업의 최상위 메타 스킬이다.** 모든 작업은 — 다른 스킬을 함께 쓰든 쓰지 않든 — mbo-천상의 목표·승인·결과 틀 위에서 진행된다.

```
                  ┌──────────────────────────┐
                  │        /mbo-천상          │ ← 최상위 (이 스킬)
                  │   목표·승인·결과 보고     │
                  └──────────┬───────────────┘
                             │ 통솔
        ┌──────────┬─────────┴─────────┬──────────┐
       [東]        [西]                [南]        [北]
    개발방법론    소대 편제           진단·감사    토론 예측
                             │
                             ▼
              코어·기본·메타·도메인 (다수)
```

mbo-천상은 작업의 **목표·승인·결과**를 책임지고, 실행은 적절한 스킬에 위임한다. **어떤 스킬을 호출할지는 천상이 작업 맥락을 보고 자율 판단한다** — 작업↔스킬 고정 매핑표를 두지 않는다. 목표만 정확히 합의되면, 그 목표를 이루는 수단은 천상이 알아서 고른다.

> **mbo-천상 없이 작업하면 목표·승인·결과가 흐트러진다.** 어떤 스킬을 쓰든 mbo-천상 위에서 시작한다.

---

> **PO의 철학:**
> "과정은 너의 마음대로 진행해도 좋다. 그러나, 나랑 합의를 해서 사전에 설정한 목표는 끝까지 추구해야 한다 — 통과시키려 기준을 낮추지 않고, 미달이면 정직하게 보고한다. 단, 품질은 최고로, 비용은 합리적 수준으로, 투입시간은 적절하게."

이 스킬의 핵심은 **자율과 책임**이다:
- **자율**: 목표 달성 방법은 AI가 자유롭게 선택한다. 과정에 간섭하지 않는다.
- **책임**: 합의된 목표는 반드시 달성한다. 변명이 아니라 결과로 보고한다.
- **품질**: 대충 끝내는 것이 아니라 최고 수준의 결과물을 만든다.
- **효율**: 불필요한 비용(토큰, 시간)을 낭비하지 않는다. 합리적으로 진행한다.

> 어떤 작업이든 실행 전에 `/mbo-천상`을 실행하면:
> 1. 현재 상황을 분석하고
> 2. 목표를 구조화하고
> 3. PO 승인을 받고 (= 합의)
> 4. 목표 기반으로 자율 실행하고
> 5. 완료 시 목표 달성 여부를 보고한다 (= 책임)

---

## 사용법

```
/mbo-천상                    — 대화 맥락에서 자동으로 목표 도출
/mbo-천상 디자인 혁신         — 특정 주제 지정
/mbo-천상 report             — 진행 중인 mbo-천상(MBO) 달성 여부 보고
```

(`/mbo`로도 호출 가능 — 영구 별칭)

---

## 용어 정의

이 스킬에서 쓰는 핵심 용어를 먼저 못 박는다 — 이후 본문은 이 정의를 따른다.

| 용어 | 정의 |
|------|------|
| **PO(인간, 지휘자)** | 목표를 합의·승인하고 최종 확인하는 사람. 이하 **PO**로 줄여 쓴다. |
| **KPI** | 목표 달성 정도를 **수치로 재는 지표**(예: p95 응답시간, 에러율). 정량 측정이 역할이며, 통과/탈락을 직접 가르지 않는다. |
| **목표 달성 평가·검증 기준** | 산출물이 목표를 달성했는지 **통과/탈락(O/X)을 가르는 규칙**. KPI 실측값을 이 기준과 대조해 판정한다. (이 문서에서 "완료 기준"·"검증 기준"·"판정 기준"을 따로 쓰지 않고 이 한 명칭으로 통일한다.) |
| **완료 판정 3조건** | 코드 산출물의 완료를 가르는 세 조건 — ①결과물 존재 ②빌드 통과 ③실제로 호출·실행됨. ③이 핵심(파일만 있고 안 쓰이면 미완료). 비코드 산출물은 별도 완료 조건을 정한다. |
| **목표서 파일** | 승인된 MBO 목표서를 저장해 추적하는 파일. 세션·컴팩션 이후에도 목표를 복원하는 근거. (구 명칭 "MBO 파일") |

> **KPI ↔ 평가·검증 기준은 섞지 않는다.** KPI는 수치를 재고, 평가·검증 기준은 그 수치로 O/X를 판정한다. 한 숫자로 뭉뚱그리면 "80점이니 대충 됐다"는 애매한 결론이 나온다.

---

## 프로세스

```
┌─────────────────────────────────────────────────────────┐
│  PHASE 1: 목표 정의 (AI가 수행)                          │
│                                                         │
│  ① 대화 맥락 분석 — 무엇을 하려는지 파악                  │
│  ② 스무고개 목표 발굴 — 20문답 기준(단축 가능)           │
│     └ 방향 미결 시 → 다관점 토론 (3 전문가+DA, 필요 시)  │
│  ③ 현재 상태(AS-IS) 조사 — 코드/파일/배포 상태 확인       │
│  ④ MBO 목표서 작성 — TO-BE·KPI·실행계획을 양식에 정리     │
│  ⑤ PO 승인 — 목표서 제시 → 승인 대기                     │
│  ┌──────────┐                                           │
│  │ 승인?    │                                           │
│  └────┬─────┘                                           │
│   Yes │  No → 수정 후 재제시                              │
│       ↓                                                  │
│  PHASE 2: 실행 (승인 후)                                 │
│                                                         │
│  ⑥ 목표서 파일 저장 (추적용)                             │
│  ⑦ 작업 실행 (Task 순서대로)                             │
│     └ 대규모 작업 시 → 소대 편제 (45명+, 필요 시)        │
│  ⑧ 중간 점검 (필요 시)                                   │
│       ↓                                                  │
│  PHASE 3: 결과 보고                                      │
│                                                         │
│  ⑨ 목표 달성 여부 대조 (AS-IS→TO-BE→실제)               │
│  ⑩ KPI 실측값 보고                                      │
│  ⑪ 미달성 항목 사유 + 후속 조치                          │
│  ⑫ PO 최종 확인                                         │
└─────────────────────────────────────────────────────────┘
```

---

## PHASE 1: 목표 정의

### Step 1: 맥락 분석

대화 내용에서 다음을 파악:
- **무엇을**: 어떤 작업/변경/개선을 하려는지
- **왜**: 동기/배경/문제점
- **범위**: 어디까지 할 것인지

### Step 2: 스무고개 목표 발굴 (목표가 막연하면 강제)

목표가 한 문장으로 또렷하지 않으면 — 막연한 요구를 그대로 목표서에 넣지 않는다. AI가 묻고 PO가 답하는 **스무고개 문답법**으로 목표를 끌어낸다.

> **스무고개 문답법**: AI가 한 번에 한 질문씩 던지고 PO가 답하며, 막연한 요구를 충분히 캐내는 기획법 — 20문답을 기준으로 하되, AI의 사전 조사로 목표가 또렷해지면 더 일찍 수렴할 수 있다.

> 첫 멘트: *"지금부터 목표 스무고개를 시작합니다. 한 번에 한 질문씩, 20문답을 기준으로 다듬되 — 중간에 목표가 또렷해지면 더 일찍 마칠 수 있습니다."*

**규칙**
- 한 번에 한 질문만. PO의 답을 받은 뒤 다음 질문으로 (AskUserQuestion 사용).
- **백지 질문 금지 — 조사 기반 제안형으로 묻는다.** 어렵거나 전문적인 사항(설계·구조·방법)은 PO에게 백지로 떠넘기지 않는다. AI가 먼저 조사·연구해 안을 만들고 "이렇게 하면 되겠습니까?"로 확인받는다. PO에게는 PO만 아는 것(진짜 목표·취향·제약·범위)만 순수하게 질문한다.
- **20문답은 기준이지 족쇄가 아니다.** 20문답을 목표로 하되 — AI의 사전 조사(AS-IS 조사·웹검색 등)로 목표·KPI·범위가 충분히 또렷해지면 20문답 전에도 종료한다. 반대로 20문답 안에 또렷해지지 않으면 20을 넘겨 계속한다.
  - **단축 조건(중간 종료 요건)**: 다음 3요소가 모두 확보되면 문답을 조기 종료한다 — ①**목표**가 한 문장으로 떨어짐 ②**범위** 경계(어디까지/어디부터 제외)가 정해짐 ③**평가·검증 기준**(무엇으로 O/X를 가를지)이 잡힘. 셋 중 하나라도 비면 단축하지 않는다.
- "요청이 명확하다"는 자가 판단으로 생략 금지 — 목표가 진짜 한 문장으로 떨어질 때만 생략한다.

**스무고개로 캐낼 것**
- 진짜 목표 — 표면 요구 너머 PO가 실제로 원하는 것
- 성공 기준 — 무엇으로 측정하면 "달성"인가 (KPI 후보)
- 범위 경계 — 어디까지 할 것인가 / 어디부터는 제외인가
- 제약 — 시간·비용·품질의 우선순위, 금지사항
- AS-IS 인식 — PO가 보는 현재 문제

스무고개가 끝나면 목표·KPI·범위가 또렷해진다 — 이것이 좋은 MBO 목표서의 재료다.

#### 방향이 안 잡히면 — 다관점 토론 (필요한 경우)

스무고개를 마쳐도 방향이 결정되지 않으면 **다관점 토론**으로 방향을 좁힌다. 같은 도메인의 전문가 3인과 반론 전담 DA(Devil's Advocate)가 토론하게 하되, 각 입장은 **독립적으로 생성**한다(한 주체가 모든 입장을 자기 입맛대로 작문하면 무효). 이때 LLM은 **단일 모델(예: Claude) 한 종류면 충분**하며(여러 프로바이더 불필요), **구독 CLI로 호출한다(API 직접 호출 금지).** 결론은 토론에 참여하지 않은 별도 판정자가 정리해 목표서의 토대로 삼는다.

**이럴 때 한다 (하나라도 해당)**
- 스무고개 종료 후에도 핵심 방향(접근법·우선순위·대안 선택)이 미결
- 대안이 2개 이상 팽팽하게 맞서 우열을 가리기 어려움
- PO가 "잘 모르겠다 / 알아서 정해봐"로 방향 판단을 위임

**기본 절차 (5단계)**
1. **쟁점 정의** — 무엇을 결정해야 하는지 한 문장으로 적는다
2. **페르소나 배정** — 입장이 다른 전문가 3인 + 반론 전담 DA. 각자 독립적으로 발언(자기작문·자기채점 금지)
3. **토론** — 각 전문가가 근거와 함께 주장하고, DA가 각 주장의 허점을 공격한다
4. **수렴** — 합의점과 남는 쟁점을 정리하고 우열을 판정한다
5. **결론** — 권고안 1개를 도출한다

토론으로 도출된 방향은 MBO 목표서에 반영해 PO 승인을 받는다 — 결정되는 것은 *방향*이고, *목표 승인*은 PO 몫이다.

### Step 3: AS-IS 조사

실제 코드/파일/시스템 상태를 확인:
- 관련 파일 Read
- 현재 동작 상태 확인 (배포 사이트, 빌드 결과 등)
- 문제점/부족한 점 정리
- 관련 코드·배포가 없거나 위치를 모르면 추측하지 말고 PO에게 코드 위치·저장소·배포 URL을 묻는다

### Step 4: MBO 목표서 작성

아래 양식으로 PO에게 제시한다. **목표서 제시 자체가 PO의 숨은 의도를 끌어내는 장치다** — 스무고개에서 드러나지 않은 진짜 목적이 목표서를 보는 순간 나오는 경우가 많다. 그때는 Step 5의 수정·재제시 루프로 목표서를 다시 다듬는다.

양식:

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  MBO 목표서 — {작업명}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 최상위 목표
"{한 줄로 정의된 목표}"

## 현재 vs 목표

| 항목 | 현재 (AS-IS) | 목표 (TO-BE) |
|------|-------------|-------------|
| ... | ... | ... |

## 측정 지표 (KPI)

| 지표 | 현재값 | 목표값 | 측정 방법 | 측정 조건 | 핵심 |
|------|--------|--------|----------|----------|:----:|
| ... | ... | ... | ... | ... | 필수/비핵심 |

- **측정 조건**: 같은 지표라도 재는 방식에 따라 값이 달라진다 — 통계 기준(평균/p95/최댓값), 표본 크기·대상, 측정 기간·환경을 명시한다. (예: "p95, 최근 7일, 프로덕션 1000req")
- **핵심**: 이 지표가 목표 달성의 **필수 조건**인지(필수) 부가 지표인지(비핵심) 표기한다 — PHASE 3의 부분달성 판정이 이 열을 근거로 갈린다.

## 산출물 (Deliverable)

작업 종료 시 실제로 나올 결과물을 구체적으로 명시한다 (파일·문서·배포 URL 등). 여기 적은 산출물 행은 PHASE 3 '산출물·검증' 표가 그대로 가져와 인도·검증 결과를 채운다.

| 산출물 | 형태 / 위치 |
|--------|------------|
| ... | ... |

## 목표 달성 평가·검증 기준

| 완료 조건 | 검증 방법 | 검증 주체 | 핵심 |
|----------|----------|----------|:----:|
| ... | ... | ... | 필수/비핵심 |

- **코드 산출물**의 완료 조건 예: 파일 존재 · 빌드 통과 · **실제로 호출·실행됨** · (UI면) 주요 버튼·링크 클릭 동작 (= 완료 판정 3조건).
- **비코드 산출물**(보고서·조사·기획·문서)은 완료 판정 3조건이 그대로 맞지 않는다 — 산출물에 맞는 완료 조건을 따로 정한다:

  | 산출물 유형 | 완료 조건 예 |
  |------------|-------------|
  | 보고서·문서 | 요구 항목 전부 포함 · 사실/수치 출처 확인 · 지정 형식 준수 |
  | 조사·리서치 | 조사 범위 전수 커버 · 근거 링크·인용 존재 · 결론이 데이터로 뒷받침 |
  | 기획·설계 | 목표·범위·리스크 명시 · 대안 비교 · 실행 가능성 검토 |

- **검증 독립성 요건 (자기검증 금지의 실체)**: 합격 판정은 작성 주체가 내리지 못한다. 검증은 ①**작업에 참여하지 않은 별도 검증자**(독립 에이전트 또는 PO)가 ②**원자료·실제 산출물에 직접 접근**해 ③목표서의 기준으로 채점한다. 작성자의 요약·자기보고만 보고 판정하는 것은 검증이 아니다.
- KPI와 구분 — **KPI는 정량 수치를 측정**, 목표 달성 평가·검증 기준은 **이진 통과(O/X)를 판정**한다.

## 자가수정 한도

미달 시 재시도(자가수정 게이트)의 상한을 미리 못 박는다 — 무한 루프·비용 폭주를 막기 위함.

| 항목 | 값 |
|------|-----|
| 재시도 상한 | (예: 3회) |
| 시간 한도 | (예: 30분 / 토큰 예산) |
| 소진 시 처리 | 미달 상태 그대로 정직 보고 — 목표·KPI는 낮추지 않음 |

## 실행 계획

| # | Task | 설명 | 예상 결과 |
|---|------|------|----------|
| 1 | ... | ... | ... |

## 리스크

| 리스크 | 대응 방안 |
|--------|----------|
| ... | ... |

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
이 목표를 승인하시겠습니까?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### Step 5: PO 승인 대기

- PO가 승인하면 PHASE 2로 진행
- PO가 수정 요청하면 목표서 수정 후 재제시
- **승인 없이 작업 시작 절대 금지**

---

## PHASE 2: 실행

### 목표서 파일 저장

승인된 목표서를 파일로 저장하여 추적한다 — 컴팩션·세션 변경 후에도 목표가 복원되도록.

저장 위치: **현재 작업 중인 디렉터리(작업 폴더) 루트**에 직접 저장한다.

```
{작업 폴더}/YYYY_MM_DD__HH.MM_mbo-천상_{작업명}.md
```

(레거시 호환: 기존 `MBO_{작업명}.md` 파일명도 인식)

- 작업 폴더 = 지금 이 작업이 진행되는 프로젝트 폴더(현재 작업 디렉터리)
- 외부 공유 폴더나 다른 문서의 규칙에 의존하지 않는다 — 저장 위치 규칙이 이 스킬 안에서 완결된다

내용: 승인된 MBO 목표서 전문 + 승인 일시

> 이하 이 파일을 **목표서 파일**이라 부른다 — PHASE 3 결과 보고를 이 파일에 덧붙인다.

### 작업 실행

- Task 목록 순서대로 실행한다.
- **일반 작업이면** 작업 → 검증 → 기록 순서로 진행한다 (기본 경로).

작업의 규모·성격에 따라, 아래 패턴을 **필요한 경우에만** 적용한다.

**대규모 멀티 에이전트가 필요한 경우 — 소대 편제**
많은 일을 병렬로 쪼개야 하면 에이전트를 3계층으로 편성한다 — 소대장 1(전략·조율·통합, 직접 작업 최소화) → 분대장 N개(영역별, 보통 3~5개) → 분대원(실제 작업) + 필요 시 외부 AI를 용병으로 투입해 교차 검증. 분대는 독립 실행 후 결과를 올리고, 소대장은 통합 전 직접 대조(grep·diff)로 실제 결과를 확인한다. (Claude Code의 Agent Teams 기능 — 구독 토큰 기반 — 으로 구현)

**구조화 개발 프로젝트인 경우 — Stage 루프**
일을 3차원 좌표(Stage·Area·Level)로 나눌 수 있으면, 먼저 전체 Task 계획을 세우고 각 Stage를 순환한다: Task 실행 → 검증 → Stage Gate(통과 판정) → 기록 → 다음 Stage. Gate를 통과해야 다음으로 넘어가며, Stage 수는 규모에 따라 동적으로 정한다. **완료 판정 3조건: ①코드 존재 ②빌드 통과 ③실제로 호출·실행됨 — ③이 핵심**(파일만 있고 아무 데서도 안 쓰면 미완료).

**산출물을 다차원으로 진단·감사해야 하는 경우 — 역방향 분해 진단**
대상(코드·문서·절차서)을 독립 단위로 분해해 "단위 × 평가항목" 그리드를 만들고, 의존성 순서대로 진단한다. 각 단위에서 진단 → 개선안 → 개선 → 재진단까지 한자리에서 완결한다. 규율: ① **런타임 우선** — 정적 분석만으로 완료 선언 금지, 실제 빌드·구동·스모크 테스트로 먼저 검증(실패 시 등급 상한 강제) ② **두 점수 분리** — '산출물 품질'과 '진단 문서 완성도'를 한 숫자로 섞지 않는다 ③ 보류한 Critical이 1건이라도 남으면 완료 선언 불가.

**미래 결과·성패를 가늠해야 하는 경우 — AI팀 구조화 토론 예측**
입장이 다른 AI 팀(낙관·중립·회의 등)과 반론 전담(DA)을 편성해 여러 단계로 토론시킨다(독립 발언 → 교차 토론 → 사실 확인 → 반론 → 수렴 판정). 각 발언의 입장·확신도·논증 품질·관련도를 가중 합산해 하나의 예측 지수(−100 ~ +100, 음수=부정 전망·양수=긍정 전망)로 정량화한다. 서로 다른 관점과 여러 종류의 LLM을 교차시켜 단일 추정보다 견고하게 만든다. **각 LLM은 구독 CLI로 호출한다(API 직접 호출 금지)** — 다만 구독 CLI가 없는 모델(예: Grok)은 부득이 API로 두는 예외를 허용한다. 토론 결과는 표·그래프로 정리해 근거를 남긴다.

**산출물 품질을 끌어올려야 하는 경우 — 점수 평가 자기수렴**
"검토 → 수정 → 평가"를 한 라운드로 묶어, 목표 점수(100점)에 도달할 때까지 최대 10회 반복한다. 명확한 다기준 루브릭(예: 정확성·완성도·구조·규칙 준수·실용성 + 사양 일치도)으로 채점한다. 핵심은 **수정한 주체와 채점하는 주체를 분리**하는 것 — 수정에 참여하지 않은 별도 검증자가 독립 채점해 자기평가 편향을 막는다. 만점이 나오면 가능하면 다른 계열 LLM(구독 CLI)으로 한 번 더 교차검증해 진짜 만점인지 확인한다.

**버그·오류를 잡아야 하는 경우 — 반복 디버깅 루프**
"문제 분석 → 원인 진단 → 수정 → 검증 → 완료 판정"을 한 사이클로 돌리고, 해결될 때까지 최대 5회 반복한다. 검증은 결과물 종류에 맞춘다 — 시각적(UI/화면)이면 스크린샷을 찍어 확인, 코드/로직이면 빌드·테스트 실행, API/서버면 응답·로그 확인. 해결되면 즉시 종료하고, 5회를 소진해도 미해결이면 회차별 시도 내역과 남은 문제·다음 접근법을 정리해 보고한다.

### 중간 점검 (선택)

대규모 작업 시 중간에 KPI 현황 보고:

```
MBO 중간 점검 — {작업명}
━━━━━━━━━━━━━━━━━━━
| 지표 | 목표 | 현재 | 달성률 |
|------|------|------|--------|
```

---

## PHASE 3: 결과 보고 (`/mbo-천상 report`)

작업 완료 시 또는 `/mbo-천상 report` 실행 시:

```markdown
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  MBO 결과 보고 — {작업명}
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

## 최상위 목표
"{목표}" → {달성/미달성/부분달성}

## 목표 달성 여부

| 항목 | AS-IS | TO-BE | 실제 결과 | 달성 |
|------|-------|-------|----------|:----:|
| ... | ... | ... | ... | O/X |

## KPI 실측

| 지표 | 목표값 | 실측값 | 달성 |
|------|--------|--------|:----:|
| ... | ... | ... | O/X |

## 산출물·검증

| 산출물 | 인도 | 검증 방법 | 검증 결과 | 검증 주체 |
|--------|:---:|----------|:--------:|----------|
| ... | O/X | ... | O/X | ... |

## 미달성 항목 (있을 경우)

| 항목 | 사유 | 후속 조치 |
|------|------|----------|
| ... | ... | ... |

## 총평
{2~3줄 요약}

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

### 달성 판정 규칙 (달성 / 부분달성 / 미달성)

최상위 목표의 판정은 다음 규칙으로 가른다 — "느낌"이 아니라 항목·KPI의 O/X 집계로 결정한다.

| 판정 | 조건 |
|------|------|
| **달성** | 목표 달성 여부 항목이 **전부 O** + 핵심 KPI가 **전부 목표값 충족** |
| **부분달성** | **핵심(필수) 항목·KPI는 모두 충족**했으나 일부 **비핵심** 항목이 미달 — 또는 목표를 정량적으로 부분 달성(예: 목표 10건 중 7건) |
| **미달성** | 핵심 항목·KPI 중 하나라도 미달 |

- **판정 근거는 목표서 양식의 "핵심" 열**이다 — KPI 표·평가·검증 기준 표의 각 행에 핵심(필수)/비핵심을 미리 표기해 두면, 위 세 판정이 그 열을 기준으로 기계적으로 갈린다("느낌" 판정 방지).
- **부분달성은 반드시 미달 항목의 사유·후속 조치를 함께 보고**한다 — 부분달성을 달성으로 반올림하지 않는다.

### 자가수정 게이트 (수렴 분기)

결과 보고 전, KPI 실측·검증 결과를 스스로 평가한다. 핵심 KPI가 목표값 미달이거나 검증이 X이면 — 단순 기록·종결이 아니라 미달 원인을 진단해 **다음 행동을 다시 고른다**:
- 산출물 품질 미달 → 별도 평가 절차로 재검토·재작업 (품질 기준 도달까지)
- 특정 Task 실패 → 그 Task만 PHASE 2로 되돌려 재실행 (전체 반복 금지)
- 방향 자체가 틀림 → PHASE 1 목표서 재제시 (**PO 재승인 필수 — 승인 게이트 우회 금지**)

재실행은 **최대 N회(권장 2~3)·비용·시간 한도 안에서**. 수렴 실패 시 미달 상태 그대로 정직하게 보고한다. **합의된 목표·KPI는 낮추지 않는다** — 루프는 목표 달성을 위한 보강이지, 목표를 깎아 통과시키는 장치가 아니다. (평가 주체는 자기검증 금지 원칙에 따라 별도 검증에 위임.)

### 결과 파일 업데이트 (`/mbo report`의 목표서 파일 탐색 규칙)

`/mbo report`는 PHASE 2에서 저장한 **목표서 파일**을 찾아 그 하단에 결과 보고를 덧붙인다. 탐색 규칙:

1. **탐색 경로**: 현재 작업 폴더(작업 디렉터리) 루트에서 찾는다. 하위 폴더는 뒤지지 않는다.
2. **파일 패턴**: `*_mbo-천상_*.md` (신규) 또는 레거시 `MBO_*.md`.
3. **복수 파일이면**: ①대화 맥락의 작업명과 일치하는 것 우선 → ②그래도 여럿이면 **가장 최근(파일명 날짜·수정시각)** 것 → ③그래도 모호하면 **추측하지 말고 PO에게 어느 목표서인지 확인**한다.
4. **하나도 없으면**: 목표서 파일이 없다는 뜻 → 결과만 임의 판정하지 말고, 목표서부터 작성(PHASE 1)해야 함을 알린다.

```
Read → {작업 폴더}/YYYY_MM_DD__HH.MM_mbo-천상_{작업명}.md
Edit → 하단에 "## 결과 보고" 섹션 추가
```

---

## 핵심 원칙

1. **합의된 목표는 끝까지 추구** — 목표 달성이 최우선. 통과시키려 기준을 낮추지 않고, 미달이면 정직하게 보고한다. 과정은 자유, 결과는 책임.
2. **품질은 최고로** — 대충 끝내는 것은 달성이 아니다. 상품성 있는 결과물이어야 한다.
3. **비용은 합리적으로** — 불필요한 서브에이전트 남발, 중복 작업, 과잉 설계를 하지 않는다.
4. **투입시간은 적절하게** — 빠르되 품질을 희생하지 않는다. 느리되 낭비하지 않는다.
5. **측정 가능한 KPI** — "좋게 만든다"가 아니라 "Lighthouse 80점 이상"처럼 수치화
6. **승인 = 합의** — PO가 목표를 이해하고 동의한 뒤에만 작업. 승인 없이 실행 금지.
7. **과정은 자율** — 목표 달성 방법은 AI가 판단. PO는 과정에 간섭하지 않는다.
8. **결과는 목표 대비** — "뭘 했다"가 아니라 "목표를 달성했다/못했다"로 보고
9. **미달성은 사유와 후속** — 실패도 기록해야 다음에 반복 안 함

### 9원칙 ↔ 12스텝 매핑

각 원칙이 프로세스의 어느 스텝에서 지켜지는지 — 원칙이 추상 구호로 뜨지 않고 스텝에 물린다.

| 핵심 원칙 | 지켜지는 스텝 |
|----------|--------------|
| 1. 합의된 목표는 끝까지 추구 | ⑤승인 → ⑦실행 → ⑨달성 대조 (전 과정 관통) |
| 2. 품질은 최고로 | ⑦작업 실행 · ⑧중간 점검 |
| 3. 비용은 합리적으로 | ⑦작업 실행(과잉 편성·중복 금지) |
| 4. 투입시간은 적절하게 | ⑦작업 실행 · ⑧중간 점검 |
| 5. 측정 가능한 KPI | ④목표서 작성(KPI 정의) · ⑩KPI 실측 |
| 6. 승인 = 합의 | ⑤PO 승인 |
| 7. 과정은 자율 | ⑥~⑧ 실행 구간(PO 불간섭) |
| 8. 결과는 목표 대비 | ⑨달성 대조 · ⑩실측 · ⑫PO 최종 확인 |
| 9. 미달성은 사유와 후속 | ⑪미달성 사유 + 후속 조치 |

---

> **스킬 자체 점검**: mbo-천상은 메타 스킬이라 별도 테스트 모드를 두지 않는다. 스킬을 점검할 때는 실제 작업 하나를 골라 PHASE 1~3 한 사이클을 돌려 검증한다.

